{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success mb-2">
        <i class="bi bi-floppy"></i>
        {{ button_label|default('Guardar canvis') }}
    </button>
{{ form_end(form) }}

{% extends 'base.html.twig' %}

{% block title %}Location{% endblock %}

{% block body %}

    <div class="row">
        <div class="col">
            <h1>Location</h1>

            <table class="table">
                <tbody>
                <tr>
                    <th>Planta</th>
                    <td>{{ location.floor }}</td>
                </tr>
                <tr>
                    <th>Consulta</th>
                    <td>{{ location.room }}</td>
                </tr>
                </tbody>
            </table>

            <a class="btn btn-success" href="{{ path('app_location_index') }}"> Tornar llistat</a>

            <a class="btn btn-primary" href="{{ path('app_location_edit', {'id': location.id}) }}">edit</a>

            {{ include('location/_delete_form.html.twig') }}
        </div>
    </div>

{% endblock %}

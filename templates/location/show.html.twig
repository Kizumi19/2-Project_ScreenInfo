{% extends 'base.html.twig' %}

{% block title %}Localització{% endblock %}

{% block inside_body %}
    <h1>Localització</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ location.id }}</td>
            </tr>
            <tr>
                <th>Planta</th>
                <td>{{ location.floor }}</td>
            </tr>
            <tr>
                <th>Consulta</th>
                <td>{{ location.room }}</td>
            </tr>
        </tbody>
    </table>


    <a class="btn btn-primary" href="{{ path('app_location_edit', {'id': location.id}) }}">Edita</a>

    {{ include('location/_delete_form.html.twig') }}
    {{ include('doctor/_back_to_list.html.twig') }}

{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Speciality{% endblock %}

{% block body %}
    <h1>Speciality</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ speciality.id }}</td>
            </tr>
            <tr>
                <th>Type_Speciality</th>
                <td>{{ speciality.TypeSpeciality }}</td>
            </tr>
            <tr>
                <th>Hidden</th>
                <td>{{ speciality.hidden ? speciality.hidden|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_only_speciality_index') }}">back to list</a>

    <a href="{{ path('app_only_speciality_edit', {'id': speciality.id}) }}">edit</a>

    {{ include('only_speciality/_delete_form.html.twig') }}
{% endblock %}

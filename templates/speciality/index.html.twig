{% extends 'base.html.twig' %}

{% block title %}Especialitats index{% endblock %}

{% block inside_body %}
    <h1>Index d'especialitats</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Tipus especialitat</th>
                <th>Accions</th>
            </tr>
        </thead>
        <tbody>
        {% for speciality in specialities %}
            <tr>
                <td>{{ speciality.typeSpeciality }}</td>
                <td>
                    <div class="btn-group">
                        <a class="btn btn-secondary" href="{{ path('app_speciality_show', {'id': speciality.id}) }}">Mostra</a>
                        <a class="btn btn-secondary" href="{{ path('app_speciality_edit', {'id': speciality.id}) }}">Edita</a>
                    </div>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">No s'han trobat resultats</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('app_speciality_new') }}">Crea una nova especilialitat</a>

{% endblock %}

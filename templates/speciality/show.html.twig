{% extends 'base.html.twig' %}

{% block title %}Speciality{% endblock %}

{% block inside_body %}
    <h1>Especialitat</h1>

    <table class="table">
        <tbody>

        <tr>
            <th>Doctor</th>
            <td>
                {% for nameDoctor in speciality.fullNamesOfDoctors %}
                    {{ nameDoctor }}
                {% endfor %}
            </td>
        </tr>
            <tr>

                <th>Tipus d'especialitat</th>
                <td>{{ speciality.TypeSpeciality }}</td>
            </tr>
        </tbody>
    </table>


    <a class="btn btn-primary" href="{{ path('app_speciality_edit', {'id': speciality.id}) }}">Edita </a>

    {{ include('speciality/_delete_form.html.twig') }}
    {{ include('speciality/_back_to_list.html.twig') }}


{% endblock %}

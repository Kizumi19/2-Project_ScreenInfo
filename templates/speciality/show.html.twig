{% extends 'base.html.twig' %}

{% block title %}Speciality{% endblock %}

{% block body %}
    <h1>Speciality</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ speciality.id }}</td>
            </tr>
            <tr>
                <th>TypeSpeciality</th>
                <td>{{ speciality.typeSpeciality }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ speciality.createdAt ? speciality.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Hidden</th>
                <td>{{ speciality.hidden ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_speciality_index') }}">back to list</a>

    <a href="{{ path('app_speciality_edit', {'id': speciality.id}) }}">edit</a>

    {{ include('speciality/_delete_form.html.twig') }}
{% endblock %}
